export type project = {
  title: string;
  description: string;
  userId: string;
  category: string;
  githubRepo: string;
  liveLink: string;
  techStack: string[];
  thumbnail: string;
};

enum techStack {
    
}
